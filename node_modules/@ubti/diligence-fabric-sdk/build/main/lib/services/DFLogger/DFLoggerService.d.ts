import { CancelablePromise } from "../../../core/CancelablePromise";
import { LogResponse } from "../../../types/DFLog/response/log-response";
import { CustomResponse } from "../../../types/api/Response";
export declare class DFLoggerService {
    private headers;
    private userId;
    private tenantId;
    private appId;
    constructor(appId: number, tenantId: number);
    setUserToken(token: string): void;
    /**
     * creates log in df.
     * @param requestBody The log request parameters.
     * @returns The log inserted success message or returns bad request
     */
    log(logLevel: string, callingFunction: string, dynmcFieldsJSON: string): CancelablePromise<CustomResponse<LogResponse>>;
}
