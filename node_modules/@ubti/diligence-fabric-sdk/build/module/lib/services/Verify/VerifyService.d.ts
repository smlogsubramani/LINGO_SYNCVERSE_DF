import { CustomResponse } from "../../../types/api/Response";
import { ValidateDomainRegistration } from "../../../types/auth/request/validate-domain-registration";
import { GetUserResponse } from "../../../types/user/response/get-user-response";
export declare class VerifyService {
    private tenantId;
    private appId;
    private appEnvironmentCODE;
    private headers;
    private userId;
    constructor(appId: number, tenantId: number, appEnvironmentCODE: string);
    setUserToken(token: string): void;
    IsEmailVerfied(emailAddress: string): Promise<CustomResponse<boolean>>;
    ReVerifyEmail(requestBody: ValidateDomainRegistration): Promise<CustomResponse<GetUserResponse>>;
    Verify(token: string): Promise<CustomResponse<GetUserResponse>>;
}
